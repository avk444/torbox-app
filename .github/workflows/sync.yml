name: Sync to Hugging Face Hub
on:
  push:
    branches: [automation-engine]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun Engine
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Heavy Compilation on GitHub Servers
        run: |
          bun install
          bun run build

      - name: Nuclear Purge of Binaries & Un-ignore Build Folders
        run: |
          # 1. Force the git tracker to include the newly compiled .next folder
          sed -i '/\.next/d' .gitignore || true

          # 2. Delete ALL image assets causing Hugging Face's security push blocks
          find . -type f \( -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.ico" -o -name "*.gif" -o -name "*.svg" \) -delete

          # 3. Clean local scratchpads before shipping
          rm -rf node_modules backend/node_modules

          # 4. Flat pack everything into a fresh commit tracking state
          rm -rf .git
          git init
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git checkout -b main
          git add .
          git commit -m "Pre-compiled text-only deployment package"

      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git push --force https://Avk44:$HF_TOKEN@huggingface.co/spaces/Avk44/torbox-manager main:main
